google-genai>=1.15.0
google-adk>=1.16.0
pydantic>=2.0.0
email-validator>=2.0.0

# 重點摘要
# - **核心概念**：專案依賴套件列表。
# - **關鍵技術**：Google GenAI SDK, Google ADK, Pydantic, Email Validator。
# - **重要結論**：定義了運行代理所需的 Python 套件及其版本要求。
# - **行動項目**：使用 `pip install -r requirements.txt` 安裝依賴。
