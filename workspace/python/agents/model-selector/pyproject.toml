[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "model-selector"
version = "0.1.0"
description = "教學 22: 模型選擇與最佳化"
requires-python = ">=3.9"
dependencies = [
    "google-adk>=1.15.1",
]
