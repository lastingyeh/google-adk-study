# Slack 機器人設定
# 請從 api.slack.com/apps 取得這些資訊

# 機器人使用者 OAuth token (以 xoxb- 開頭)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Socket Mode 的應用程式層級 token (以 xapp- 開頭)
SLACK_APP_TOKEN=xapp-your-app-token-here

# 用於請求驗證的 Slack 簽署密鑰 (Signing Secret)
SLACK_SIGNING_SECRET=your-signing-secret-here

# Google API 設定
# 請從 https://makersuite.google.com/app/apikey 取得
GOOGLE_API_KEY=your-gemini-api-key-here

# 選用：Vertex AI 設定 (如果使用 Vertex AI 而非 Gemini Developer API)
# GOOGLE_GENAI_USE_VERTEXAI=false
# GOOGLE_CLOUD_PROJECT=your-project-id
# GOOGLE_CLOUD_LOCATION=us-central1

# HTTP 伺服器的連接埠 (用於 Cloud Run 生產環境)
PORT=8080

# 環境：development (開發) 或 production (生產)
ENVIRONMENT=development

# 重點摘要 (.env.example)
# - 核心概念：環境變數設定範本，用於儲存敏感資訊與設定參數。
# - 關鍵技術：dotenv (.env 檔案格式)。
# - 重要結論：
#   - 包含 Slack 認證 (Bot Token, App Token) 與 Google API Key。
#   - 開發者必須將此檔案複製為 `.env` 並填入實際數值。
# - 行動項目：複製此檔案至 `.env` 並設定正確的 API 金鑰。
