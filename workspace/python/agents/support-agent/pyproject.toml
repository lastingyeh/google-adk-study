[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "support-agent"
version = "0.1.0"
description = "教學 10：評估與測試 - 客戶支援代理"
requires-python = ">=3.9"
dependencies = [
    "google-adk>=1.15.1",
]