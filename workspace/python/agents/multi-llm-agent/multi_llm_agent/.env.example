# 教學 28: 透過 LiteLLM 使用其他 LLM
# 環境變數範本

# ==============================================================================
# 📝 說明
# 1. 將此檔案複製到 `.env` -> `cp .env.example .env`
# 2. 在 `.env` 檔案中填入您的 API 金鑰
# 3. .env 檔案會被自動載入
# ==============================================================================

# ------------------------------------------------------------------------------
# Google (Gemini) API 金鑰
#
# ⭐️ 建議用於 ADK 的預設模型
# 取得免費金鑰：https://aistudio.google.com/app/apikey
# ------------------------------------------------------------------------------
GOOGLE_API_KEY="your_google_api_key_here"

# ------------------------------------------------------------------------------
# OpenAI API 金鑰
#
# 用於 GPT-4o, GPT-4o-mini 等模型
# 取得金鑰：https://platform.openai.com/api-keys
# ------------------------------------------------------------------------------
OPENAI_API_KEY="sk-your_openai_api_key_here"

# ------------------------------------------------------------------------------
# Anthropic (Claude) API 金鑰
#
# 用於 Claude 3.7 Sonnet, Haiku 等模型
# 取得金鑰：https://console.anthropic.com/
# ------------------------------------------------------------------------------
ANTHROPIC_API_KEY="sk-ant-your_anthropic_api_key_here"

# ------------------------------------------------------------------------------
# Ollama (本地模型) 設定
#
# 如果您的 Ollama 伺服器在不同的主機或連接埠上執行，請取消註解並修改此行
# 預設為：http://localhost:11434
# ------------------------------------------------------------------------------
# OLLAMA_API_BASE="http://localhost:11434"

# ------------------------------------------------------------------------------
# 其他 LiteLLM 支援的供應商
#
# LiteLLM 支援超過 100 種 LLM 供應商。
# 請將對應供應商的 API 金鑰加到此處。
# 範例：
#
# export TOGETHER_API_KEY="..."
# export HUGGINGFACE_API_KEY="..."
# export REPLICATE_API_TOKEN="..."
# ------------------------------------------------------------------------------
