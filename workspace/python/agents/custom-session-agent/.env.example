# Google ADK 配置
GOOGLE_API_KEY=在此輸入您的_google_api_key

# Redis 會話 (Session) 後端
REDIS_HOST=localhost
REDIS_PORT=6379
SESSION_SERVICE_URI=redis://localhost:6379

# ADK 配置
ADK_MODEL=gemini-2.5-flash

### 重點摘要
- **核心概念**：環境變數設定。
- **關鍵技術**：Google API Key, Redis 連線設定, ADK 模型選擇。
- **重要結論**：本檔案定義了 Agent 運作所需的金鑰與服務位址。
- **行動項目**：將此檔案複製為 `.env` 並填入實際的金鑰。
