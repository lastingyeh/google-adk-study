[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-agent"
version = "0.1.0"
description = "enterprise-agent: Gemini Enterprise - 企業級 Agent 平台"
requires-python = ">=3.9"
dependencies = [
    "google-adk>=1.15.1",
]
