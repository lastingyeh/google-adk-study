歡迎來到這堂高階實踐課程。我是你們的資深技術導師。

在探討過 ADK 的底層架構後，我們必須進入最具代表性的實戰應用場景：**Shopper's Concierge（個人化電商導購）**。這不只是一個搜尋框的升級，它是將 AI 從「被動工具」轉化為「主動代理程式（AI Agent）」的典範轉移。在雙向串流的脈絡下，Shopper's Concierge 展示了多模態互動如何徹底消除購物摩擦。

---

### 📌 Shopper's Concierge 學習地圖

1.  **從搜尋到意圖**：解構模糊語意搜尋的技術核心。
2.  **視覺環境感知**：AI 如何透過「眼睛」主動觀察物理世界。
3.  **深度研究模式**：Multi-agent 協作與自動化分類決策。
4.  **代碼即真理**：分析電商導購中的多模態上游傳遞邏輯。
5.  **知識收斂**：總結未來電商代理程式的四大技術支柱。

---

### 一、 場景驅動教學：重塑零售互動體驗

我們透過三個關鍵展示場景，來看看雙向串流如何解決傳統電商的痛點。

#### 💡 場景 1：解決「說不清楚」的模糊語意搜尋
**提問：** 「導師，如果使用者只記得杯子上有『跳舞的人』，傳統關鍵字搜尋往往會失敗，ADK 是如何處理的？」
**解析：**
**關鍵在於對話式意圖與向量空間的結合。** 使用者不再需要精確的商品名稱。
*   **互動表現**：使用者要求尋找「印有跳舞的人的杯子」，AI 能從千萬件商品中識別視覺特徵，找到具有波蘭民俗舞者設計的復古馬克杯,。
*   **這代表什麼？** 這背後仰賴的是 Gemini 模型對大規模數據的跨模態理解能力，將口語描述轉化為視覺特徵進行檢索,。

#### 💡 場景 2：從被動等待到「主動視覺建議」
**提問：** 「電商 AI 如何能像百貨公司的專櫃人員一樣，根據我當下的環境給予建議？」
**解析：**
這是 Shopper's Concierge 最驚艷的部分：**視覺環境感知 (Visual Awareness)**,。
*   **技術行為**：AI 以每秒一張圖 (1 FPS) 的頻率處理影像序列,。它能辨識出使用者的居家辦公空間（書桌、黑椅子、筆電）。
*   **主動出擊**：AI 會主動說出：「我看到一個居家辦公室的配置，桌上有筆電... 你可能會對螢幕架或檯燈有興趣」。這種從物理世界提取上下文資訊的能力，觸發了更精確的推薦。

#### 💡 場景 3：高效率的「深度研究 (Deep Research)」
**提問：** 「當我幫小孩選禮物卻面對 75 件商品感到困惑時，AI 如何縮短我的決策路徑？」
**解析：**
**這代表了 Multi-agent 協作的自動化。** AI 不只是列出清單，而是進行歸納與分類。
*   **行為模式**：AI 會利用 Google 搜尋獲取市場趨勢，將商品自動分類為 STEM 套裝、動作公仔等類別，並推送「管家精選 (Concierge's Pick)」,。
*   **技術核心**：AI 先執行外部研究代理（Research Agent），再將結果回傳給搜尋代理，實現決策流程自動化。

---

### 二、 邏輯具象化：傳統電商 vs. AI 導購管家

為了強化視覺記憶，我們將 Shopper's Concierge 的技術演進整理如下：

| 特性         | 傳統搜尋引擎 (Legacy) | AI 導購管家 (Concierge)            |
| :----------- | :-------------------- | :--------------------------------- |
| **輸入模式** | 精確關鍵字 (Keywords) | 自然語言、語音語調、即時影像,      |
| **行為邏輯** | 被動檢索 (Reactive)   | 主動觀察、環境感知、深度研究,      |
| **處理深度** | 列出商品連結          | 歸納、分類、詳細描述產品風格匹配度 |
| **數據規模** | 標準資料庫檢索        | 千萬級數據量的即時 RAG 結合        |

---

### 三、 代碼即真理：多模態輸入的上游處理

在 Shopper's Concierge 的架構中，開發者必須處理來自 Web 端的多模態資料。以下是來源資料中 `bidi-demo` 對於影像與文字傳遞的實作註解：

```python
# [導師點評]：這是 Phase 3 的上游核心。
# Shopper's Concierge 需要並行處理文字意圖與攝影機影格。

async def upstream_task() -> None:
    """從 WebSocket 接收訊息並發送到 LiveRequestQueue。"""
    while True:
        message = await websocket.receive()

        if "text" in message:
            json_message = json.loads(message["text"])

            # [關鍵在此]：發送文字意圖（如：幫我找跳舞的人的杯子）
            if json_message.get("type") == "text":
                content = types.Content(parts=[types.Part(text=json_message["text"])])
                live_request_queue.send_content(content) #,

            # [關鍵在此]：發送視覺快照（環境感知）
            elif json_message.get("type") == "image":
                # 將 Base64 影格解碼回原始位元組
                image_data = base64.b64decode(json_message["data"])
                # 使用 send_realtime 傳遞 Blob 數據
                image_blob = types.Blob(mime_type="image/jpeg", data=image_data)
                live_request_queue.send_realtime(image_blob) #
```
*(參考來源：)*

**導師劃線註解：**
*   影像輸入建議頻率為 **1 FPS**，解析度建議為 **768x768**，這足以應付大多數生活場景的理解,。
*   使用 `send_realtime()` 而非 `send_content()` 來傳遞影像 Blob，這能確保數據是以即時模式進行串流傳輸,。

---

### 四、 知識延伸與收斂

Shopper's Concierge 展現了雙向串流技術在商業應用中的極致潛力。

**實戰導向總結：**
1.  **跨模態融合**：AI 不僅讀取數據表，更能將商品圖片與規格進行融合，扮演專業導購員。
2.  **規模感與性能**：在 **1,000 萬件商品** 中即時檢索，證明了 RAG 與語音 API 結合的高性能。
3.  **情緒感知 (Affective Dialogue)**：在面對挫折感強的客戶（如產品瑕疵投訴）時，原生音訊模型能調整回應邏輯，增強共情能力,。
4.  **技術利器**：透過 ADK，開發者能將繁雜的連線管理、工具編排與視覺處理封裝，大幅縮短開發週期,。

這就是未來的電商體驗：一個有眼睛、能對話、且具備專業研究能力的私人管家。

#ShoppersConcierge #MultiModalAI #VisualAwareness #DeepResearch #EcommerceAgent

**標籤：** #ShoppersConcierge #MultiModalAI #VisualAwareness #DeepResearch #EcommerceAgent